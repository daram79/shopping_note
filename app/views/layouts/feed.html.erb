<!DOCTYPE html>
<html>
<head>
  <title>Sns</title>
  <meta name="viewport" content="width=device-width,initial-scale=1.0,minimum-scale=1.0,maximum-scale=1.0">
  <link rel="stylesheet" href="http://code.jquery.com/mobile/1.4.3/jquery.mobile-1.4.3.min.css" />
  <script src="http://code.jquery.com/jquery-1.11.1.min.js"></script>
  <script src="http://code.jquery.com/mobile/1.4.3/jquery.mobile-1.4.3.min.js"></script>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<% if notice %>
  <p class="alert alert-notice"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-error"><%= alert %></p>
<% end %>
<p class="navbar-text pull-right">
<% if user_signed_in? %>
  Logged in as <strong><%= current_user.email %></strong>.
  <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
  <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
  <div data-role="page">
    <%= yield %>
    <div data-role="footer" data-position="fixed">
	  	<div data-role="navbar">
	  		<ul>
	  			<li><a href="#" class="ui-btn-active" data-icon="grid">feed</a></li>
	  			<li><a id="heart" href="#" data-icon="heart">heart</a></li>
	  			<li>
	  				<!--a href="/feeds/write.html" data-icon="plus">heart</a-->
	  				<%= link_to 'write', new_feed_path, remote: true , data:{icon: 'plus' } %>
	  			</li>
	  			<li><a id="search" href="#" data-icon="search">search</a></li>
	  			<li><a id="setting" href="#" data-icon="gear">setting</a></li>
	  		</ul>
	  	</div>
	  </div>
		<!--div class="menu" style="">
			<div class="feed" style="">feed</div>
			<div class="heart" style="">heart</div>
			<div class="new" style="">
				<%= link_to '+', new_feed_path, style: "text-decoration: none;" %>
			</div>
			<div class="search" style="">search</div>
			<div class="my" style="">my</div>
		</div-->
	<% else %>
	  <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
	  <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
	<% end %>
  </div>

</body>
</html>
