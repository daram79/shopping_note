<%= form_for(@profile_photo, html: {data: {ajax: 'false'}}) do |f| %>
  <% if @profile_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_photo.errors.count, "error") %> prohibited this profile_photo from being saved:</h2>

      <ul>
      <% @profile_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :user_id %>
  <div>프로필 사진:
  	<%= f.file_field :image, accept: "image/*" %>
  </div>

  <div class="actions">
    <%= f.submit "업로드" %>
  </div>
<% end %>
<div data-role="footer" data-position="fixed" data-tap-toggle="false">
  <div data-role="navbar">
	<ul>
	  <li>
	  	<%= link_to 'feed', feeds_path, data:{icon: 'grid' } %>
	  </li>
	  <li><a id="heart" href="#" data-icon="heart">heart</a></li>
	  <li>
	  	<%= link_to 'write', new_feed_path, data:{icon: 'plus' } %>
	  </li>
	  <li><a id="search" href="#" data-icon="search">search</a></li>
      <li>
      	<%= link_to 'setting', settings_path, class: "ui-btn-active", data:{icon: 'gear' } %>
      </li>
    </ul>
  </div>
</div>

<script>
	$( "#change_profile_photo" ).click(function() {
	  $(".edit_profile_photo").submit();
	});
</script>